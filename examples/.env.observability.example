# Copy to examples/.env.observability (same folder as docker-compose.yml)
# cp .env.observability.example .env.observability

VITE_OBSERVABILITY_STACK=true
VITE_OTEL_ENDPOINT=http://localhost:4318/v1/traces
VITE_FARO_ENDPOINT=http://localhost:12347/collect
